DATABASE_URL=mysql://deployer:{{ deployer_db_password | default('deployer') }}@{{ wp_db_host }}:{{ wp_db_port | default(3306) }}/deployer?ssl-mode=required
LISTEN_ADDR=127.0.0.1:{{ deployer_port | default(3001) }}
SEMAPHORE_URL=http://127.0.0.1:3000
SEMAPHORE_USER={{ semaphore_admin_user | default('admin') }}
SEMAPHORE_PASSWORD={{ semaphore_admin_password }}
SEMAPHORE_PROJECT_ID={{ deployer_semaphore_project_id | default(1) }}
RUST_LOG=deployer=info
